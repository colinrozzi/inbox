[package]
name = "inbox-smtp-handler"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
packr-guest = "0.10.2"
base64 = { version = "0.22", default-features = false, features = ["alloc"] }
