[workspace]
members = ["acceptor", "api-handler", "mailbox", "mailbox-router", "opsctl", "smtp-acceptor", "smtp-handler", "tenant-registry"]
resolver = "2"

[profile.release]
opt-level = "s"
lto = true
